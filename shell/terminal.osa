#!/usr/bin/osascript
on run argv
    set dir to quoted form of (first item of argv)
    tell app "Terminal"
        activate
        set shell to do script "cd " & dir
    end tell
    do shell script "echo ok"
end run
